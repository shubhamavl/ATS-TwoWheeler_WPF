<UserControl x:Class="ATS_TwoWheeler_WPF.Views.Controls.AppStatusBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ATS_TwoWheeler_WPF.Views.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="800">
    <StatusBar Background="{StaticResource PrimaryBlue}" Foreground="White">
        <StatusBarItem>
            <TextBlock Text="{Binding StatusText}" />
        </StatusBarItem>
        <Separator/>
        <StatusBarItem>
            <TextBlock Text="{Binding StreamStatusText}" />
        </StatusBarItem>
        <Separator/>
        <StatusBarItem>
            <StackPanel Orientation="Horizontal">
                <Ellipse Width="8" Height="8" Fill="#FFDC3545" Margin="0,0,5,0"/>
                <TextBlock Text="TX:" Margin="0,0,5,0"/>
                <TextBlock Text="{Binding TxCount}" />
            </StackPanel>
        </StatusBarItem>
        <Separator/>
        <StatusBarItem>
            <TextBlock Text="{Binding RxCount, StringFormat='RX: {0}'}" />
        </StatusBarItem>
        <Separator/>
        <StatusBarItem HorizontalAlignment="Right">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding TimestampText}" />
            </StackPanel>
        </StatusBarItem>
        <Separator/>
        <StatusBarItem HorizontalAlignment="Right">
            <TextBlock Text=" 2025 AVL India Pvt Ltd. All rights reserved." FontSize="10" Opacity="0.8"/>
        </StatusBarItem>
    </StatusBar>
</UserControl>
