<UserControl x:Class="ATS_TwoWheeler_WPF.Views.Controls.SettingsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ATS_TwoWheeler_WPF.Views.Controls"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="400" Background="White">
    <UserControl.Resources>
        <!-- Style resources -->
        <Style TargetType="GroupBox">
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="BorderBrush" Value="#FFDDDDDD"/>
        </Style>
        
        <Style TargetType="TextBlock" x:Key="LabelStyle">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Width" Value="140"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#FF2C3E50"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Application Settings" FontSize="18" FontWeight="Bold" Margin="15,10" Foreground="#FF2980B9"/>

        <TabControl Grid.Row="1" Margin="10,0,10,10" Background="Transparent" BorderThickness="0,1,0,0">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Padding" Value="15,8"/>
                    <Setter Property="FontSize" Value="13"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </Style>
            </TabControl.Resources>

            <!-- Tab 1: Signal Processing -->
            <TabItem Header="Signal Processing">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="10">
                        <GroupBox>
                            <GroupBox.Header>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Weight Filtering" VerticalAlignment="Center"/>
                                    <Button Content="ℹ️" Command="{Binding ShowHelpCommand}" CommandParameter="Filter" 
                                            Background="Transparent" BorderThickness="0" Margin="10,0,0,0" Cursor="Hand" ToolTip="Help"/>
                                </StackPanel>
                            </GroupBox.Header>
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                    <TextBlock Text="Filter Type:" Style="{StaticResource LabelStyle}"/>
                                    <ComboBox ItemsSource="{Binding FilterTypes}" 
                                              SelectedItem="{Binding FilterType}"
                                              Width="120"/>
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                    <StackPanel.Style>
                                        <Style TargetType="StackPanel">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding FilterType}" Value="EMA">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </StackPanel.Style>
                                    <TextBlock Text="Alpha (Smoothing):" Style="{StaticResource LabelStyle}"/>
                                    <TextBox Text="{Binding FilterAlpha}" Width="60" Margin="0,0,5,0"/>
                                    <TextBlock Text="(0.0 - 1.0)" VerticalAlignment="Center" Foreground="Gray" FontSize="10"/>
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                     <StackPanel.Style>
                                        <Style TargetType="StackPanel">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding FilterType}" Value="SMA">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </StackPanel.Style>
                                    <TextBlock Text="Window Size:" Style="{StaticResource LabelStyle}"/>
                                    <TextBox Text="{Binding FilterWindowSize}" Width="60"/>
                                </StackPanel>
                                
                                <CheckBox Content="Enable Filtering" IsChecked="{Binding FilterEnabled}" Margin="140,5,0,0"/>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Tab 2: Display & UI -->
            <TabItem Header="Display &amp; UI">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="10">
                        <GroupBox>
                            <GroupBox.Header>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Performance &amp; Accuracy" VerticalAlignment="Center"/>
                                    <Button Content="ℹ️" Command="{Binding ShowHelpCommand}" CommandParameter="General" 
                                            Background="Transparent" BorderThickness="0" Margin="10,0,0,0" Cursor="Hand" ToolTip="Help"/>
                                </StackPanel>
                            </GroupBox.Header>
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                    <TextBlock Text="Decimal Places:" Style="{StaticResource LabelStyle}"/>
                                    <ComboBox SelectedItem="{Binding WeightDisplayDecimals}" Width="60">
                                        <sys:Int32>0</sys:Int32>
                                        <sys:Int32>1</sys:Int32>
                                        <sys:Int32>2</sys:Int32>
                                    </ComboBox>
                                </StackPanel>
                                
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                    <TextBlock Text="UI Update Rate (ms):" Style="{StaticResource LabelStyle}"/>
                                    <TextBox Text="{Binding UIUpdateRateMs}" Width="60"/>
                                </StackPanel>
                                
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                    <TextBlock Text="Data Timeout (s):" Style="{StaticResource LabelStyle}"/>
                                    <TextBox Text="{Binding DataTimeoutSeconds}" Width="60"/>
                                </StackPanel>
                            </StackPanel>
                        </GroupBox>

                        <GroupBox Header="Visibility Options">
                            <StackPanel>
                                <CheckBox Content="Show Raw ADC Values" IsChecked="{Binding ShowRawADC}" Margin="0,0,0,5"/>
                                <CheckBox Content="Show Streaming Indicators" IsChecked="{Binding ShowStreamingIndicators}" Margin="0,0,0,5"/>
                                <CheckBox Content="Show Calibration Icons" IsChecked="{Binding ShowCalibrationIcons}" Margin="0,0,0,5"/>
                                <CheckBox Content="Show Quality Metrics (R²)" IsChecked="{Binding ShowCalibrationQualityMetrics}"/>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Tab 3: Calibration -->
            <TabItem Header="Calibration">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="10">
                        <GroupBox Header="Calibration Strategy">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                                    <TextBlock Text="Processing Mode:" Style="{StaticResource LabelStyle}"/>
                                    <ComboBox ItemsSource="{Binding CalibrationModes}" 
                                              SelectedItem="{Binding CalibrationMode}"
                                              Width="120"/>
                                </StackPanel>
                                
                                <Grid Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <Border Grid.Column="0" BorderBrush="#FFCCCCCC" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,0,5,0">
                                        <StackPanel>
                                            <TextBlock Text="Internal ADC" FontWeight="Bold" Margin="0,0,0,5" Foreground="#FF2980B9"/>
                                            <TextBlock Text="{Binding InternalCalStatus}" Margin="0,0,0,2"/>
                                            <TextBlock Text="{Binding InternalCalDate}" FontSize="10" Foreground="Gray" Margin="0,5,0,5"/>
                                            <Button Content="Reset" Command="{Binding ResetCalibrationInternalCommand}" 
                                                    Background="#FFE74C3C" Foreground="White" Padding="5,2" Style="{x:Null}"/>
                                        </StackPanel>
                                    </Border>
                                    
                                    <Border Grid.Column="1" BorderBrush="#FFCCCCCC" BorderThickness="1" CornerRadius="4" Padding="10" Margin="5,0,0,0">
                                        <StackPanel>
                                            <TextBlock Text="ADS1115" FontWeight="Bold" Margin="0,0,0,5" Foreground="#FF2980B9"/>
                                            <TextBlock Text="{Binding AdsCalStatus}" Margin="0,0,0,2"/>
                                            <TextBlock Text="{Binding AdsCalDate}" FontSize="10" Foreground="Gray" Margin="0,5,0,5"/>
                                            <Button Content="Reset" Command="{Binding ResetCalibrationADS1115Command}" 
                                                    Background="#FFE74C3C" Foreground="White" Padding="5,2" Style="{x:Null}"/>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </StackPanel>
                        </GroupBox>

                        <GroupBox Header="Advanced Calibration Logic">
                            <StackPanel>
                                <CheckBox Content="Multi-Sample Averaging" IsChecked="{Binding CalibrationAveragingEnabled}" Margin="0,0,0,5"/>
                                <StackPanel Margin="20,5,0,0" IsEnabled="{Binding CalibrationAveragingEnabled}">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                        <TextBlock Text="Samples:" Style="{StaticResource LabelStyle}" Width="80"/>
                                        <TextBox Text="{Binding CalibrationSampleCount}" Width="60"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                        <TextBlock Text="Duration (ms):" Style="{StaticResource LabelStyle}" Width="80"/>
                                        <TextBox Text="{Binding CalibrationCaptureDurationMs}" Width="60"/>
                                    </StackPanel>
                                    <CheckBox Content="Use Median Filter" IsChecked="{Binding CalibrationUseMedian}" Margin="0,5"/>
                                    <CheckBox Content="Remove Outliers" IsChecked="{Binding CalibrationRemoveOutliers}" Margin="0,5"/>
                                    <StackPanel Orientation="Horizontal" Margin="20,0,0,5" IsEnabled="{Binding CalibrationRemoveOutliers}">
                                        <TextBlock Text="Threshold (σ):" Width="100"/>
                                        <TextBox Text="{Binding CalibrationOutlierThreshold}" Width="50"/>
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Tab 4: System -->
            <TabItem Header="System">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="10">
                        <GroupBox Header="Data Management">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                    <TextBlock Text="Log Format:" Style="{StaticResource LabelStyle}"/>
                                    <ComboBox ItemsSource="{Binding LogFormats}" 
                                              SelectedItem="{Binding LogFileFormat}"
                                              Width="80"/>
                                </StackPanel>
                                <Button Content="Open Data Directory" Command="{Binding OpenDataDirectoryCommand}" Margin="0,5"/>
                                <Button Content="View Settings File" Command="{Binding OpenSettingsFileCommand}" Margin="0,5"/>
                            </StackPanel>
                        </GroupBox>

                        <GroupBox Header="About">
                            <StackPanel>
                                <TextBlock Text="ATS Two-Wheeler WPF Interface" FontWeight="Bold"/>
                                <TextBlock Text="Version: 1.2.0-MVP" Foreground="Gray"/>
                                <TextBlock Text="Restore Legacy UI Project" Foreground="Gray" FontStyle="Italic"/>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
        </TabControl>

        <!-- Footer -->
        <Border Grid.Row="2" Background="#FFF1F3F5" Padding="15,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Save Settings" Command="{Binding SaveSettingsCommand}" 
                        Style="{x:Null}" Background="#FF28A745" Foreground="White" Width="120"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
